linters:
  enable:
    - errcheck
    - goconst
    - revive
    - gosec
    - govet
    - ineffassign
    - lll
    - megacheck
    - misspell
    - unconvert
    - unused
  enable-all: false

run:
  timeout: 10m

issues:
  exclude-files:
    - schema.go
    - pulumiManifest.go
